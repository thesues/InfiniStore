//RDMA read/write request
// allow len(blocks_size) == 1 and len(rkey) == 1
table RemoteMetaRequest {
  keys: [string];
  block_size: [uint];
  rkey: [uint];                  // rkey
  remote_addrs: [ulong];       // GPU addresses
  op: byte;
}
root_type RemoteMetaRequest;
